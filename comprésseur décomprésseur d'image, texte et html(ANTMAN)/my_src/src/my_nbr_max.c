/*
** EPITECH PROJECT, 2022
** my_free_array.c
** File description:
** returns maximal between a and b;
*/

#include "../include/my.h"

int my_nbr_max(int a, int b)
{
    if (a > b) {
        return (a);
    } else {
        return (b);
    }
}
